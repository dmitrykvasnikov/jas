const DEFAULTS = {
  slidesToShow: 1,
  slidesToScroll: 1,
  gap: '0px',
  loop: false,
  direction: 'row',
  active: 1,
  // index of active slide relative to parent wrapper
  activeIndex: 1,
  prev: 1,
  next: 1,
  speed: '500ms',
  offsetX: '0px',
  offsetY: '0px',
  overflow: 'hidden'
}

const NAVIGATION = {
  prev: '.jas-button-prev',
  next: '.jas-button-next',
}

export {
  DEFAULTS, NAVIGATION
}